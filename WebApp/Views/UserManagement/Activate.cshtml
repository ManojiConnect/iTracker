@model WebApp.Controllers.UserViewModel
@{
    ViewData["Title"] = "Activate User";
    Layout = "_Layout";
}

<div class="container-fluid p-4">
    <div class="row">
        <div class="col-md-6 mx-auto">
            <div class="card shadow-sm border-success">
                <div class="card-header bg-success text-white">
                    <h4 class="mb-0">Confirm Activation</h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <i class="fas fa-info-circle me-2"></i>
                        Are you sure you want to activate this user? This action will allow the user to access the system again.
                    </div>

                    <div class="mb-4">
                        <h5 class="mb-3">User Details</h5>
                        <div class="row mb-2">
                            <div class="col-md-4 fw-bold">Name:</div>
                            <div class="col-md-8">@Model.FirstName @Model.LastName</div>
                        </div>
                        <div class="row mb-2">
                            <div class="col-md-4 fw-bold">Email:</div>
                            <div class="col-md-8">@Model.Email</div>
                        </div>
                        <div class="row mb-2">
                            <div class="col-md-4 fw-bold">Role:</div>
                            <div class="col-md-8">
                                <span class="badge @(Model.Role == "Admin" ? "bg-danger" : "bg-primary")">
                                    @Model.Role
                                </span>
                            </div>
                        </div>
                    </div>

                    <form asp-controller="UserManagement" asp-action="ActivateConfirmed" method="post">
                        <input type="hidden" name="id" value="@Model.Id" />
                        <div class="d-flex justify-content-between">
                            <a asp-controller="UserManagement" asp-action="Index" class="btn btn-secondary">
                                <i class="fas fa-arrow-left me-2"></i> Back to List
                            </a>
                            <button type="submit" class="btn btn-success">
                                <i class="fas fa-check me-2"></i> Activate User
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div> 